# 精益PRD文档 - 第1批
产品：用药提醒（微信小程序）
V0.1 | Rev1.0 | 2026-03-18

---

## 文档信息

| 项目 | 内容 |
|------|------|
| **产品名称** | 用药提醒 |
| **产品版本** | V0.1 (MVP) |
| **文档版本** | Rev1.0 |
| **目标平台** | 微信小程序 |
| **创建日期** | 2026-03-18 |

## 产品概述

**产品定位：** 专为长期服药人群（老年人、慢性病患者）设计的用药提醒与打卡工具
**核心价值：** 到点推送微信订阅消息 + 一键打卡记录 + 历史随时可查
**目标用户：** 老年人、慢性病患者、长期服药人群及其家属
**主要功能：** 药品管理、服药时间设置、订阅消息提醒、服药打卡、历史查看

## 批次信息

| 项目 | 内容 |
|------|------|
| **当前批次** | 第1批 / 共2批 |
| **本批次页面** | PG-01 ~ PG-07，共7页 |
| **累计完成** | 7页 / 总计16页 |
| **整体进度** | 44% |

---

## 业务页面设计

---

### PG-01 首页（今日用药）

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 首页（今日用药） |
| **小程序路径** | pages/index/index |
| **导航路径** | 启动直接进入（底部 Tab-1：今日用药） |
| **Tab 图标** | 药瓶/提醒铃（激活态蓝色，默认态灰色） |

---

#### 2. 页面字段

**顶部信息区：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 今日日期 | 格式：X月X日 星期X | 文本 | 系统时间 |
| 订阅状态栏 | 显示剩余订阅次数，格式："今日剩余提醒次数：N次" | 文本+图标 | 本地存储 |
| 订阅警示 | 当剩余次数≤今日剩余任务数时，显示橙色警示条 | 警示条 | 计算得出 |

**今日任务列表（每条任务字段）：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 计划时间 | 格式：HH:MM | 文本 | 本地存储 |
| 药品名称 | 药品名称，最多20字，超出省略 | 文本 | 本地存储 |
| 单次剂量 | 如"1片"、"5ml" | 文本 | 本地存储 |
| 时间备注 | 如"早餐后"、"空腹"，无则不显示 | 文本（可选） | 本地存储 |
| 任务状态 | 待打卡 / 已服药 / 手动补记 / 漏服 | 状态标签 | 本地存储 |
| 打卡按钮 | 状态为"待打卡"时显示"已服药"按钮 | 按钮 | — |

**底部激励区：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 连续打卡提示 | 展示当前最长连续服药天数，如"坚持服药 · 第14天" | 文本 | 本地存储计算 |

---

#### 3. 主要功能

**F-06：今日用药任务展示**
- **优先级：** P0
- **功能描述：** 展示当天所有待打卡及已完成的服药任务，按计划时间升序排列
- **触发条件：** 进入首页时自动加载

**F-07：服药打卡**
- **优先级：** P0
- **功能描述：** 点击"已服药"按钮完成打卡，记录实际打卡时间，每个任务每日只能打卡一次
- **触发条件：** 点击任务卡片上的"已服药"按钮

**F-08：手动补记打卡（当日）**
- **优先级：** P0
- **功能描述：** 对当日已过时间但未收到提醒（未通过订阅消息进入）的任务，在首页直接补记
- **触发条件：** 点击"已服药"按钮（功能与F-07共用同一入口）

**F-10：订阅次数显示**
- **优先级：** P0
- **功能描述：** 顶部展示剩余订阅消息次数；次数不足今日任务数时显示橙色警示条
- **触发条件：** 页面加载时自动展示

---

#### 4. 交互细节

**任务列表展示：**
- 按计划时间升序排列（早的在上）
- 已过计划时间且状态为"待打卡"的任务：文字保持正常颜色，提示用户需补记
- 未到计划时间的任务：显示浅灰色时间，说明"还未到时间"
- 当日无任务时：显示 ST-02 空状态，提示"今天没有待服药任务，前往添加药品"

**任务卡片状态：**
- **待打卡：** 白底卡片，右侧显示蓝色"已服药"按钮（大号，方便老年人点击，高度≥44pt）
- **已服药：** 卡片背景变淡绿色，右侧显示绿色✓图标 + "已服药"文字，按钮不可再点击
- **手动补记：** 与已服药相同样式，但添加"（手动）"标注
- **漏服：** 卡片背景变淡红色，右侧显示红色"漏服"标签

**打卡操作反馈：**
- 点击"已服药"按钮后：按钮立即变为灰色（防误触），0.3秒后卡片状态动画切换至"已服药"
- Toast 提示："✓ 打卡成功！"，显示1.5秒后消失
- 若重复点击（已打卡的任务）：Toast 提示："该任务今日已完成打卡"

**订阅状态栏：**
- 次数充足（>今日任务数）：绿色小圆点 + "提醒正常"文字，低调展示
- 次数不足：橙色警示条，文字："今日还有X次提醒无法推送，点击去补充授权"，整条可点击
- 次数=0：红色警示条，文字："今日提醒次数已用完，点击补充授权"

**连续打卡提示：**
- 首次使用（无历史）：不显示
- 有记录后：显示最长连续天数
- 当天打卡完成后连续天数加1，即时更新

**页面加载：**
- 进入页面时短暂显示 ST-01（加载中，<500ms），本地读取完成后展示任务列表
- 每次从其他页面返回首页，自动刷新任务列表状态（pageShow 事件触发）

---

#### 5. 跳转逻辑

**正常流程：**

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 点击底部"药品管理" Tab | PG-02 药品列表 | switchTab |
| 点击底部"历史记录" Tab | PG-09 服药历史 | switchTab |
| 点击底部"设置" Tab | PG-13 设置页 | switchTab |
| 点击订阅警示条 | PG-08 订阅授权引导 | navigateTo |
| 点击空状态的"添加药品"按钮 | PG-03 添加药品 | switchTab → Tab-2（PG-02），再自动弹出PG-03） |

**异常流程：**

| 异常场景 | 处理方式 |
|---------|---------|
| 本地数据读取失败 | Toast 提示"数据加载失败，请重试"，显示重试按钮 |
| 重复打卡 | Toast 提示"该任务今日已完成打卡"，不跳转 |
| 今日无任务 | 显示 ST-02 空状态，引导前往药品管理 |

---

#### 6. 页面入参

首页为 Tab 根页面，无页面入参。每次 onShow 事件触发时从本地存储重新加载数据。

---

#### 7. 页面出参

| 数据操作 | 说明 | 写入时机 |
|---------|---------|---------|
| 打卡记录 | 写入：{药品ID, 日期, 计划时间, 状态="已服药", 实际时间} | 点击"已服药"成功后 |
| 订阅消息授权 | 触发微信订阅消息授权弹窗（通过 PG-08 处理） | 点击警示条跳转 PG-08 后处理 |

**系统状态变化：**
- 打卡成功后：本地存储中对应任务状态更新为"已服药"
- 连续打卡天数：打卡成功后实时重新计算

---

### PG-02 药品列表页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 药品列表页 |
| **小程序路径** | pages/medicine/list |
| **导航路径** | 底部 Tab-2：药品管理（直接进入） |

---

#### 2. 页面字段

**导航栏：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 页面标题 | "药品管理" | 文本 | 静态 |
| 添加按钮 | 导航栏左侧或页面右上角"+"图标 | 按钮 | — |

**药品卡片列表（每条）：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 药品名称 | 最多20字，超出截断+省略号 | 文本 | 本地存储 |
| 服药次数说明 | 如"每日3次"，根据时间计划数量计算 | 文本 | 本地存储计算 |
| 下次服药时间 | 今日下一个未打卡的时间点，如"下次：14:00 午餐后" | 文本 | 本地存储计算 |
| 今日完成状态 | 今日所有任务均已打卡时显示"今日已完成 ✓" | 状态标签 | 本地存储计算 |
| 右箭头 | 指示可进入详情 | 图标 | — |

---

#### 3. 主要功能

**F-药品列表展示**
- **优先级：** P0
- **功能描述：** 展示用户已添加的所有药品，每张卡片显示药品名、服药频次、下次服药时间
- **触发条件：** 进入 Tab-2 时自动加载

**F-01入口：添加药品**
- **优先级：** P0
- **功能描述：** 点击"+"进入添加药品页（PG-03）
- **触发条件：** 点击页面右上角"+"按钮

---

#### 4. 交互细节

**列表展示：**
- 按添加时间倒序排列（最新添加的在最上方）
- 无药品时显示 ST-02 空状态：插画 + "还没有添加药品" + 大号"添加药品"按钮
- 药品数量≥5时列表可滚动

**药品卡片：**
- 点击卡片任意区域→进入 PG-05（药品详情）
- 今日所有任务已打卡→卡片右上角显示绿色"今日已完成 ✓"标签
- 今日有任务未完成→显示"下次：HH:MM 时间备注"
- 无服药时间计划（刚添加药品尚未设时间）→显示橙色提示"尚未设置服药时间"

**添加按钮：**
- 位置：导航栏标题左侧（注意：右侧为微信系统胶囊按钮区域，不可放自定义按钮）
- 或：页面内容区底部固定"+ 添加药品"按钮（推荐，更易被老年用户发现）

**从其他页面返回时：**
- 每次 onShow 时自动刷新列表（覆盖新增/编辑/删除的变化）

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 点击"+"或"添加药品"按钮 | PG-03 添加药品 | navigateTo |
| 点击任意药品卡片 | PG-05 药品详情 | navigateTo（传入药品ID） |
| 切换到其他 Tab | 对应 Tab 根页面 | switchTab |

---

#### 6. 页面入参

Tab 根页面，无入参。每次 onShow 从本地存储刷新数据。

---

#### 7. 页面出参

本页面为列表导航页，无直接数据出参。操作结果通过跳转子页面处理后返回刷新。

---

### PG-03 添加药品页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 添加药品页 |
| **小程序路径** | pages/medicine/add |
| **导航路径** | PG-02 → 点击"+"或"添加药品"按钮 |

---

#### 2. 页面字段

| 字段名称 | 字段说明 | 字段类型 | 是否必填 | 数据来源 |
|---------|---------|---------|---------|---------|
| 药品名称 | 如"苯磺酸氨氯地平"，最多20字 | 文本输入框 | 是 | 用户输入 |
| 单次剂量 | 如"1片"、"5ml"、"10mg"，最多10字 | 文本输入框 | 是 | 用户输入 |
| 备注 | 如"饭后服用"、"不能与牛奶同服"，最多30字 | 文本输入框 | 否 | 用户输入 |
| 保存按钮 | 提交保存药品信息 | 按钮 | — | — |

---

#### 3. 主要功能

**F-01：添加药品**
- **优先级：** P0
- **功能描述：** 录入药品名称、单次剂量、备注并保存；保存成功后自动跳转至 PG-06 设置服药时间
- **触发条件：** 点击"保存"按钮，且名称和剂量均已填写

---

#### 4. 交互细节

**输入框行为：**
- 药品名称框：
  - 获得焦点：边框高亮蓝色
  - 失去焦点：边框恢复灰色
  - 超过20字：阻止继续输入，右侧显示"20/20"字数提示（红色）
  - 清空按钮：有内容时右侧显示"×"
- 单次剂量框：
  - 同上，最多10字
  - Placeholder：如"例：1片、5ml"
- 备注框：
  - 选填，Placeholder："如：饭后服用（选填，最多30字）"
  - 多行文本框，最多2行展示

**保存按钮状态：**
- 默认/药品名称或剂量未填写：按钮灰色，不可点击
- 药品名称和剂量均已填写：按钮蓝色，可点击
- 点击后：按钮文字变为"保存中..."，防止重复提交
- 保存成功：Toast "✓ 药品添加成功"，0.8秒后跳转 PG-06
- 保存失败（本地存储异常）：Toast "保存失败，请重试"，按钮恢复

**引导提示：**
- 页面顶部说明文字："添加药品后，请在下一步设置服药时间"（小字，灰色）

**字号要求：**
- 标签文字：16px；输入内容：18px（老年用户，字号宜大）

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 保存成功 | PG-06 服药时间管理（新增药品的时间管理） | redirectTo（替换当前页，避免返回到添加页） |
| 点击导航栏左侧"返回"按钮 | PG-02 药品列表 | navigateBack |
| 点击导航栏左侧"取消"（若有） | PG-02 药品列表 | navigateBack |

---

#### 6. 页面入参

| 参数名称 | 参数说明 | 是否必传 | 默认值 |
|---------|---------|---------|-------|
| 无 | 新增页面，无需传入参数 | — | — |

---

#### 7. 页面出参

| 参数名称 | 参数说明 | 传递目标 | 传递时机 |
|---------|---------|---------|---------|
| medicineId | 新建药品的唯一ID（本地生成） | PG-06（服药时间管理） | 保存成功跳转时 |
| medicineName | 药品名称（用于 PG-06 页面标题展示） | PG-06 | 保存成功跳转时 |

**系统状态变化：**
- 本地存储新增一条药品记录：{id, 名称, 剂量, 备注, 创建时间}

---

### PG-04 编辑药品页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 编辑药品页 |
| **小程序路径** | pages/medicine/edit |
| **导航路径** | PG-05 药品详情 → 点击"编辑"按钮 |

---

#### 2. 页面字段

与 PG-03 完全相同（表单结构一致），但所有字段预填充已有数据：

| 字段名称 | 字段说明 | 是否必填 | 数据来源 |
|---------|---------|---------|---------|
| 药品名称 | 回填已有名称，可修改 | 是 | 本地存储（回填） |
| 单次剂量 | 回填已有剂量，可修改 | 是 | 本地存储（回填） |
| 备注 | 回填已有备注，可修改 | 否 | 本地存储（回填） |
| 保存按钮 | 保存修改 | — | — |

---

#### 3. 主要功能

**F-02：编辑药品**
- **优先级：** P0
- **功能描述：** 修改已有药品的名称、剂量、备注并保存，保存后返回 PG-05 显示更新后内容
- **触发条件：** 点击"保存"按钮

---

#### 4. 交互细节

- 与 PG-03 交互逻辑完全一致（字段校验、按钮状态、字数限制）
- 区别：页面标题显示为"编辑药品"（PG-03 为"添加药品"）
- 保存成功：Toast "✓ 修改已保存"，0.8秒后返回 PG-05
- 未做任何修改直接返回：直接返回 PG-05，不提示

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 保存成功 | PG-05 药品详情（显示更新内容） | navigateBack |
| 返回按钮 | PG-05 药品详情 | navigateBack |

---

#### 6. 页面入参

| 参数名称 | 参数说明 | 是否必传 | 来源页面 |
|---------|---------|---------|---------|
| medicineId | 要编辑的药品ID，用于读取数据回填 | 是 | PG-05 |

---

#### 7. 页面出参

| 参数名称 | 参数说明 | 传递目标 | 传递时机 |
|---------|---------|---------|---------|
| 无（通过本地存储通信） | 保存后直接更新本地存储，PG-05 onShow 重新读取 | PG-05 | 返回时 PG-05 自动刷新 |

**系统状态变化：**
- 本地存储中对应药品记录的名称、剂量、备注字段更新

---

### PG-05 药品详情页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 药品详情页 |
| **小程序路径** | pages/medicine/detail |
| **导航路径** | PG-02 药品列表 → 点击任意药品卡片 |

---

#### 2. 页面字段

**药品信息区：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 药品名称 | 大号字体展示 | 文本（18px+） | 本地存储 |
| 单次剂量 | 如"1片" | 文本 | 本地存储 |
| 备注 | 如"饭后服用"，无备注则不显示该行 | 文本（可选） | 本地存储 |
| 连续服药天数 | 如"已连续服药 · 14天"，无记录则不显示 | 文本 | 本地存储计算 |

**服药时间计划区：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 区块标题 | "服药时间" | 文本 | 静态 |
| 时间计划列表 | 每条：HH:MM + 时间备注（如"早餐后"） | 列表 | 本地存储 |
| 无时间提示 | 尚未设置时间时显示"尚未设置服药时间" + 橙色样式 | 提示文本 | — |
| 管理时间按钮 | 蓝色文字链接样式，如"管理服药时间 >" | 按钮 | — |

**操作区：**

| 字段名称 | 字段说明 | 字段类型 |
|---------|---------|---------|
| 编辑按钮 | 修改药品信息 | 按钮 |
| 删除按钮 | 删除该药品，红色文字 | 按钮（危险操作样式） |

---

#### 3. 主要功能

**F-药品详情展示**
- **优先级：** P0
- **功能描述：** 展示药品的完整信息（名称、剂量、备注、所有服药时间计划、连续天数）
- **触发条件：** 进入页面时自动加载

**F-02入口：编辑药品**
- **优先级：** P0
- **功能描述：** 跳转至编辑页修改药品基本信息
- **触发条件：** 点击"编辑"按钮

**F-03：删除药品**
- **优先级：** P0
- **功能描述：** 弹出确认弹窗，二次确认后删除药品及其全部时间计划和历史打卡记录
- **触发条件：** 点击"删除"按钮

**F-04入口：管理服药时间**
- **优先级：** P0
- **功能描述：** 跳转至服药时间管理页，添加/编辑/删除时间计划
- **触发条件：** 点击"管理服药时间"按钮

---

#### 4. 交互细节

**时间计划列表：**
- 按时间升序展示（最早的在上方）
- 每条时间计划：左侧"HH:MM"大号时间 + 右侧"时间备注"小字
- 无时间计划时：显示橙色提示框"尚未设置服药时间，点击下方按钮添加"

**删除药品交互：**
- 点击红色"删除该药品"文字按钮
- 弹出 Modal 确认框：
  - 标题："确认删除？"
  - 内容："删除后，该药品的所有服药时间计划和历史打卡记录将一并删除，且不可恢复"
  - 按钮：【取消】（灰色）+ 【确认删除】（红色）
- 点击"确认删除"：执行删除，Toast "药品已删除"，跳转回 PG-02
- 点击"取消"：关闭弹窗，留在当前页

**页面刷新：**
- 每次 onShow 时重新读取本地存储，保证从 PG-04 返回时内容是最新的

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 点击"编辑"按钮 | PG-04 编辑药品 | navigateTo（传入 medicineId） |
| 点击"管理服药时间" | PG-06 服药时间管理 | navigateTo（传入 medicineId, medicineName） |
| 删除确认成功 | PG-02 药品列表 | navigateBack（弹出当前页）|
| 导航栏左侧返回 | PG-02 药品列表 | navigateBack |

---

#### 6. 页面入参

| 参数名称 | 参数说明 | 是否必传 | 来源页面 |
|---------|---------|---------|---------|
| medicineId | 要展示详情的药品ID | 是 | PG-02 |

---

#### 7. 页面出参

无直接出参，操作（编辑/删除）均通过本地存储更新，上级页面在 onShow 时自动刷新。

**系统状态变化（删除操作）：**
- 本地存储删除：该药品记录 + 该药品的所有时间计划 + 该药品的所有历史打卡记录

---

### PG-06 服药时间管理页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 服药时间管理页 |
| **小程序路径** | pages/schedule/list |
| **导航路径** | ① PG-03 添加药品保存成功后自动跳转；② PG-05 药品详情 → 点击"管理服药时间" |

---

#### 2. 页面字段

**导航栏：**

| 字段名称 | 字段说明 | 数据来源 |
|---------|---------|---------|
| 页面标题 | "{药品名称} · 服药时间"，药品名超长时截断 | 入参 medicineName |

**时间计划列表（每条）：**

| 字段名称 | 字段说明 | 字段类型 | 数据来源 |
|---------|---------|---------|---------|
| 时间 | HH:MM，大号字体 | 文本 | 本地存储 |
| 时间备注 | 如"早餐后"，无则显示"—" | 文本 | 本地存储 |
| 删除按钮 | 每条右侧红色"删除"文字按钮 | 按钮 | — |

**底部操作区：**

| 字段名称 | 字段说明 | 字段类型 |
|---------|---------|---------|
| 已添加计划数 | 如"已添加 2/6 个时间"（上限6个） | 提示文本 |
| 添加时间按钮 | "+ 添加服药时间"，未达到上限时显示 | 按钮 |

---

#### 3. 主要功能

**F-时间计划列表展示**
- **优先级：** P0
- **功能描述：** 展示该药品的所有服药时间计划，按时间升序排列
- **触发条件：** 进入页面时自动加载

**F-04：添加服药时间**
- **优先级：** P0
- **功能描述：** 点击"添加服药时间"按钮跳转至 PG-07 新增时间计划
- **触发条件：** 点击"+ 添加服药时间"按钮，且当前计划数量 < 6

**F-05：删除服药时间**
- **优先级：** P0
- **功能描述：** 删除某个时间计划，该时间点的历史打卡记录保留，仅停止未来任务
- **触发条件：** 点击某条时间计划右侧"删除"按钮

---

#### 4. 交互细节

**无时间计划时（刚保存药品后进入）：**
- 显示 ST-02 空状态：文字"还没有设置服药时间，点击下方按钮添加"
- 底部显示大号"+ 添加服药时间"按钮
- 页面顶部提示条："药品已保存，请设置服药时间以开启提醒"（橙色提示条）

**删除时间计划：**
- 点击"删除"后弹出 Modal：
  - 内容："删除后，该时间点的历史打卡记录仍然保留，但从明天起将不再提醒。确认删除？"
  - 按钮：【取消】+ 【确认删除】（红色）
- 删除成功：列表即时更新，Toast "已删除"

**已达上限（6个）：**
- "添加服药时间"按钮变为灰色禁用态
- 按钮旁提示："已达最大数量（6个）"

**时间冲突提示（同一药品同一时间）：**
- 从 PG-07 返回时，若新增时间与已有时间完全相同：Toast 提示"该时间已存在，请勿重复添加"，仍保留已添加的数据（以最新一条为准）

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 点击"+ 添加服药时间" | PG-07 添加/编辑服药时间（mode=add） | navigateTo（传入 medicineId） |
| 导航栏返回 | ① 若来自PG-03（新添加药品）：PG-05；② 若来自PG-05：PG-05 | navigateBack |

---

#### 6. 页面入参

| 参数名称 | 参数说明 | 是否必传 | 来源页面 |
|---------|---------|---------|---------|
| medicineId | 药品ID，用于读取/写入时间计划 | 是 | PG-03 / PG-05 |
| medicineName | 药品名称，用于页面标题显示 | 是 | PG-03 / PG-05 |

---

#### 7. 页面出参

无直接出参，时间计划的增删操作直接写入本地存储，PG-05 在 onShow 时自动刷新。

**系统状态变化（删除操作）：**
- 本地存储删除对应时间计划记录
- 今日对应任务（若尚未打卡）从首页任务列表移除
- 历史打卡记录保留（不受影响）

---

### PG-07 添加/编辑服药时间页

#### 1. 页面路径

| 项目 | 内容 |
|------|------|
| **页面名称** | 添加/编辑服药时间页 |
| **小程序路径** | pages/schedule/edit |
| **导航路径** | ① PG-06 → 点击"+ 添加服药时间"（新增模式）；② PG-06 → 点击某条时间的"编辑"（编辑模式，P1 支持） |

---

#### 2. 页面字段

| 字段名称 | 字段说明 | 字段类型 | 是否必填 | 数据来源 |
|---------|---------|---------|---------|---------|
| 服药时间 | 时:分 选择，使用微信原生时间选择器 | 时间选择器（Picker） | 是 | 用户选择 |
| 时间备注 | 如"早餐后"、"空腹"、"睡前"，最多10字 | 文本输入框 | 否 | 用户输入 |
| 保存按钮 | 提交保存 | 按钮 | — | — |

---

#### 3. 主要功能

**F-04：设置服药时间**
- **优先级：** P0
- **功能描述：** 选择时间（时:分）并可选填时间备注，保存后返回 PG-06 列表
- **触发条件：** 点击"保存"按钮，且时间已选择

---

#### 4. 交互细节

**时间选择器：**
- 使用微信原生 Picker 组件（mode="time"），滚轮式选择
- 默认值：新增时默认当前时间；编辑时默认已有时间
- 时间格式：HH:MM（24小时制）
- 展示区域：点击时间显示区域唤起 Picker

**时间备注输入框：**
- Placeholder："如：早餐后、空腹、睡前（选填，最多10字）"
- 超过10字：阻止继续输入，右侧显示"10/10"（红色）

**保存按钮：**
- 时间已选择→按钮蓝色可点击
- 时间未选择→按钮灰色不可点击
- 点击保存→"保存中..."防重复，成功后返回 PG-06

**快捷备注标签（可选，提升易用性）：**
- 页面提供常用时间备注快捷标签："早餐前"、"早餐后"、"午餐后"、"晚餐后"、"睡前"、"空腹"
- 点击标签即填入备注输入框（可替换），方便老年用户选择

**重复时间提示（保存前校验）：**
- 若所选时间与该药品已有时间完全相同：弹出 Modal 确认："您已设置过 HH:MM 的服药时间，是否仍然保存？"

---

#### 5. 跳转逻辑

| 触发条件 | 目标页面 | 跳转方式 |
|---------|---------|---------|
| 保存成功 | PG-06 服药时间管理（列表刷新） | navigateBack |
| 导航栏返回/取消 | PG-06 服药时间管理 | navigateBack |

---

#### 6. 页面入参

| 参数名称 | 参数说明 | 是否必传 | 来源页面 |
|---------|---------|---------|---------|
| medicineId | 药品ID，保存时关联到该药品 | 是 | PG-06 |
| mode | "add"（新增）或 "edit"（编辑） | 是 | PG-06 |
| scheduleId | 时间计划ID，仅 mode=edit 时传入 | 否（编辑时必传） | PG-06 |
| defaultTime | 编辑时回填的默认时间，格式"HH:MM" | 否（编辑时必传） | PG-06 |
| defaultNote | 编辑时回填的时间备注 | 否 | PG-06 |

---

#### 7. 页面出参

| 操作 | 说明 | 传递目标 | 传递时机 |
|---------|---------|---------|---------|
| 新增时间计划 | 写入本地存储：{id, medicineId, time, note, 创建时间} | PG-06（onShow 刷新） | 保存成功 navigateBack 时 |
| 编辑时间计划 | 更新本地存储中对应记录的 time 和 note | PG-06（onShow 刷新） | 保存成功 navigateBack 时 |

**系统状态变化：**
- 新增：本地存储新增时间计划记录，今日（若当前时间未过该时间点）自动生成对应打卡任务
- 编辑：更新时间计划记录，今日未来任务同步更新时间点

---

## 完整度自查（第1批）

| 检查项 | 状态 |
|--------|------|
| 本批7个页面均使用7部分格式 | ✅ |
| 所有页面有明确入口和出口 | ✅ |
| 交互细节含输入框状态/按钮状态/成功失败反馈 | ✅ |
| 微信导航栏右侧无自定义按钮 | ✅ |
| 无技术实现细节（无代码/API/数据库） | ✅ |
| 老年用户设计考量（大字体/大按钮/字数说明） | ✅ |
| **第1批完整度** | **100%** ✅ |
