# 业务流程分析
产品：用药提醒（微信小程序）
版本：v1.0 | 2026-03-18

---

## 一、核心业务对象

| 业务对象 | 描述 | 关键属性 |
|----------|------|----------|
| **药品** | 用户录入的一种药 | 名称、单次剂量、备注（如"饭后服用"） |
| **服药时间计划** | 某种药在某时间点服用的安排 | 所属药品、时间点（时:分）、时间备注（如"早餐后"） |
| **打卡记录** | 某个时间计划在某天的执行记录 | 所属药品、日期、计划时间、状态、实际时间 |
| **打卡状态** | 每条打卡记录的状态枚举 | 待打卡 / 已服药 / 手动补记 / 漏服 / 未记录 |
| **订阅消息配额** | 用户剩余可推送的订阅消息次数 | 剩余次数（本地估算） |

---

## 二、核心业务流程

### 流程1：首次使用——添加药品并开启提醒

```
用户进入小程序
    ↓
首页显示空状态（无药品）
    ↓
用户点击"添加药品"
    ↓
填写药品信息（名称、剂量、备注）
    ↓ [保存]
进入服药时间设置
    ↓
添加至少一个时间点（时间 + 时间备注）
    ↓ [可继续添加多个时间点]
    ↓ [完成]
系统生成今日起的打卡计划
    ↓
首页今日任务更新，显示新增药品的待打卡任务
    ↓
引导用户授权订阅消息（若尚未授权）
    ↓ [用户点击授权]
    → [授权成功] → 订阅配额更新，提醒正式生效
    → [授权拒绝] → 提示"将无法收到微信推送提醒，可从设置页补充授权"
```

**关键业务规则：**
- 药品必须有至少一个时间计划，才会产生今日打卡任务
- 药品信息保存与时间设置是两个步骤（先保存药品，再添加时间）
- 今日任务只显示"当前时间之后"或"今天已到时间"的任务

---

### 流程2：日常提醒与打卡

```
系统到达用户设置的服药时间点
    ↓
推送微信订阅消息（内容：药品名 + 剂量 + 时间备注）
    ↓
用户收到微信通知，点击进入小程序
    ↓
首页显示今日任务列表，目标任务高亮显示
    ↓
用户点击"已服药"
    ↓
打卡记录写入：状态="已服药"，实际时间=当前时间
    ↓
任务状态更新为"已完成" ✓
    ↓
[可选] 查看连续服药天数更新
```

**关键业务规则：**
- 每个时间点任务只能打卡一次；重复点击提示"今日该任务已打卡"
- 打卡不限于收到提醒后操作，随时可在首页手动打卡
- 打卡截止时间：当日23:59，过后自动归档为"未记录"状态

---

### 流程3：手动补记打卡

```
用户发现今日某任务已服药但未打卡（未通过提醒入口）
    ↓
进入首页今日任务 或 历史记录页
    ↓
找到目标任务（状态为"待打卡"）
    ↓
点击"手动补记"
    ↓
系统记录：状态="手动补记"，补记时间=当前时间
    ↓
任务标记为已完成
```

**关键业务规则：**
- 今日任务：当日23:59前可随时手动补记
- 历史任务：过去任意日期的"未记录"任务可在历史页补记，状态标注"补记"
- 一旦补记成功不可撤销（防止随意修改历史数据）

---

### 流程4：漏服标记

```
用户确认某天某药确实漏服了
    ↓
进入历史记录页，找到对应日期
    ↓
找到状态为"待打卡"或"未记录"的任务
    ↓
点击"标记漏服"
    ↓
系统记录：状态="漏服"，标记时间=当前时间
    ↓
连续服药天数重新计算（该药的连续计数中断）
```

**关键业务规则：**
- 漏服标记不可撤销
- 当日过了23:59仍未打卡、未补记、未漏服标记 → 系统自动标记为"未记录"（灰色，不影响连续计数计算，等待用户主动处理）
- 连续服药天数中断条件：该药某天有"漏服"记录（"未记录"不中断，给用户次日补记的机会）

---

### 流程5：订阅消息续期授权

```
用户打开小程序
    ↓
系统检查剩余订阅次数（本地估算）
    ↓
[次数充足] → 首页正常展示，订阅状态为绿色
    ↓
[次数不足/为0] → 首页显示订阅状态警告
    ↓
用户点击"去补充授权"
    ↓
进入订阅授权引导页
    ↓
展示说明（为什么需要授权、订阅消息是什么）
    ↓
用户点击"立即授权"→ 触发微信订阅消息授权弹窗
    ↓
[用户点击"允许"] → 订阅次数+N（根据授权药品×天数估算）
[用户点击"拒绝"] → 提示"拒绝后将无法收到提醒，可随时回来补充"
```

**关键业务规则：**
- 微信一次性订阅：用户每次点击"允许"授权1个药品模板1次推送机会
- 建议设计：每次打开小程序时，自动批量引导授权"未来3天的提醒"
- 订阅次数本地估算方式：记录"今日剩余预计可推送次数" = 用户主动授权次数 - 已推送次数
- 次数显示仅为估算，实际以微信服务端为准

---

### 流程6：编辑/删除药品

```
用户进入药品列表
    ↓
选择某药品进入详情
    ↓
[编辑] → 进入编辑页 → 修改名称/剂量/备注 → 保存 → 返回详情页，数据更新
    ↓
[管理时间] → 进入时间管理页 → 添加/删除时间点
    ↓
[删除药品] → 弹出确认弹窗："删除后该药品的所有历史记录也将删除，确认删除？"
    → [确认] → 删除药品及其所有时间计划和历史打卡记录
    → [取消] → 返回，不做操作
```

**关键业务规则：**
- 删除药品是不可逆操作，必须二次确认
- 删除药品后，历史记录一并清除（不保留孤立记录）
- 删除某个时间点时，该时间点的历史记录保留（仅删除未来任务）

---

## 三、业务规则汇总

| 规则编号 | 规则描述 |
|----------|----------|
| BR-01 | 药品必须有至少1个时间计划才能产生打卡任务 |
| BR-02 | 同一时间点同一药品每天只能打卡一次 |
| BR-03 | 打卡截止时间为当日23:59，过后任务状态变为"未记录" |
| BR-04 | 漏服标记不可撤销，连续服药天数计算中断 |
| BR-05 | "未记录"状态不中断连续天数，等待用户主动处理 |
| BR-06 | 连续服药天数 = 从最近一次漏服到今天的连续打卡天数 |
| BR-07 | 删除药品时同步删除其时间计划和历史打卡记录 |
| BR-08 | 删除某时间计划时，历史记录保留，仅停止未来任务 |
| BR-09 | 手动补记和漏服标记均不可撤销 |
| BR-10 | 订阅次数为本地估算值，仅供参考 |

---

## 四、异常处理规则

| 异常场景 | 处理方式 |
|----------|----------|
| 用户拒绝订阅消息授权 | 保存"未授权"状态，首页软提示引导，不强制 |
| 订阅消息次数为0 | 首页红色警示，引导用户打开小程序主动授权 |
| 网络异常时授权操作 | Toast提示"网络异常，请稍后重试"，不关闭页面 |
| 同一时间点重复打卡 | Toast提示"您已完成该任务的打卡" |
| 大量药品/时间点性能 | 本地存储，单用户数据量极小，无性能问题 |
| 手机时间被手动修改 | 以手机本地时间为准，不做防篡改处理（个人工具无需） |
| 跨天时（凌晨0点） | 系统刷新今日任务列表，归档昨日未处理任务为"未记录" |

---

## 五、流程闭环检查

| 流程 | 起点 | 终点 | 异常分支覆盖 | 闭环 |
|------|------|------|-------------|------|
| 首次使用流程 | 打开空白小程序 | 提醒已生效 | 授权拒绝分支 | ✅ |
| 日常打卡流程 | 收到提醒 | 任务完成 | 重复打卡 | ✅ |
| 手动补记流程 | 发现未打卡 | 记录为已服药 | 历史补记 | ✅ |
| 漏服标记流程 | 确认漏服 | 标记完成 | 不可撤销说明 | ✅ |
| 订阅续期流程 | 次数不足警告 | 授权完成 | 用户拒绝分支 | ✅ |
| 编辑/删除流程 | 进入药品管理 | 数据更新 | 删除确认弹窗 | ✅ |

---

## 六、完整度自查

| 维度 | 覆盖情况 | 完整度 |
|------|----------|--------|
| 核心业务流程 | 6个流程，全覆盖 | 100% |
| 业务规则 | 10条，含打卡/漏服/删除/订阅 | 100% |
| 异常处理 | 7个异常场景，均有处理方案 | 100% |
| 流程闭环 | 6个流程均可端到端执行 | 100% |
| 无技术实现细节 | 无数据库/API/代码内容 | ✅ |
| **总体完整度** | | **≥95%** ✅ |
